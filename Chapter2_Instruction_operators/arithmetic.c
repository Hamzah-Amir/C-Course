#include <stdio.h>

int main() {
    int a = 10,  b = 13,  c = 3,  d = 7;
    int e = a + b;
    printf("The sum of %d + %d is %d\n", a, b, e);
    // Modulus operator is used to get remainder
    printf("The remainder when b is divided by d is %d", b%d);
    return 0;
}